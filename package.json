{
  "name": "laika",
  "private": true,
  "packageManager": "pnpm@9.15.4",
  "scripts": {
    "build": "pnpm -r --if-present build",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "dev": "pnpm -r --parallel --filter @laika/webview --filter laika dev"
  },
  "engines": {
    "node": ">=20"
  }
}
